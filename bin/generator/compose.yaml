services:
  app:
    user: ${UID:-1000}:${GID:-1000}
    build:
      context: .
    volumes:
      - ./:/app
      - ../../:/app/mnt-workspace
      - /opt/venv
      - $HOME/.vscode-oss:$HOME/.vscode-oss
    network_mode: host
